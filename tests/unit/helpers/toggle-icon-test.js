import { toggleIcon } from 'dummy/helpers/toggle-icon';
import { module, test } from 'qunit';

module('Unit | Helper | toggle icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toggleIcon([42]);
  assert.ok(result);
});
