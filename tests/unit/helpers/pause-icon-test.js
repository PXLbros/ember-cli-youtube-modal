import { pauseIcon } from 'dummy/helpers/pause-icon';
import { module, test } from 'qunit';

module('Unit | Helper | pause icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pauseIcon([42]);
  assert.ok(result);
});
