import { closeIcon } from 'dummy/helpers/close-icon';
import { module, test } from 'qunit';

module('Unit | Helper | close icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = closeIcon([42]);
  assert.ok(result);
});
