import { secondsToMinutes } from 'dummy/helpers/seconds-to-minutes';
import { module, test } from 'qunit';

module('Unit | Helper | seconds to minutes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = secondsToMinutes([42]);
  assert.ok(result);
});
