import { playIcon } from 'dummy/helpers/play-icon';
import { module, test } from 'qunit';

module('Unit | Helper | play icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = playIcon([42]);
  assert.ok(result);
});
