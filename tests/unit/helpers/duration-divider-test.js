import { durationDivider } from 'dummy/helpers/duration-divider';
import { module, test } from 'qunit';

module('Unit | Helper | duration divider');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = durationDivider([42]);
  assert.ok(result);
});
