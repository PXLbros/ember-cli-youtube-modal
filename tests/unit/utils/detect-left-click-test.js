import detectLeftClick from 'dummy/utils/detect-left-click';
import { module, test } from 'qunit';

module('Unit | Utility | detect left click');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = detectLeftClick();
  assert.ok(result);
});
