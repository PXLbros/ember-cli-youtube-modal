import detectLeftButton from 'dummy/utils/detect-left-button';
import { module, test } from 'qunit';

module('Unit | Utility | detect left button');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = detectLeftButton();
  assert.ok(result);
});
