import prefixVisibilityChange from 'dummy/utils/prefix-visibility-change';
import { module, test } from 'qunit';

module('Unit | Utility | prefix visibility change');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = prefixVisibilityChange();
  assert.ok(result);
});
